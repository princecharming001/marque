# RALPH-UX-PROGRESS — UX overhaul: 7 product fixes

Progress tracker for the Ralph loop executing `docs/PLAN-UX-FIXES.md`.
**Legend:** `[ ]` todo · `[~]` in progress · `[x]` done (+ one line of evidence) · `[!]` blocked (+ reason).

**Environment note:** running on the dev Mac (not Linux CI): backend verify = `.venv/bin/pytest`
(system python3 is 3.9 and fails collection), Swift CAN be compiled here via `cd ios && ./scripts/dev.sh build`
— stronger than mirror-review. Keyless remains the CI contract for the backend suite.

**Baseline (recorded at orientation, off main f6e1bf9):** backend `pytest` keyless **569 passed** · `python -m eval.edl_eval` **PASS (5 good, 8 bad)** — no dimension may regress below this.

---

## Slice 1 — iOS quick wins

- [x] UX-C1 — DONE. `Clip.renderLocalPath` (optional-with-default) + `isServerRendered`/`playbackLocalPath`/`playbackRemoteURL` extension in Models.swift; ClipDetailSheet player (+.id incl. renderLocalPath), shareURL, both LocalThumbnail sites gated; drafts/imported stay local-first (imported's remoteURL is its raw upload — correctly out of scope). Evidence: BUILD SUCCEEDED 0 errors; journey trace: server-rendered clip → cached render or remote stream, raw take never plays.
  - spec: Library playback gating: `Clip.renderLocalPath` field + extension (`isServerRendered` = jobId != nil && source != "imported" && remoteURL non-empty; `playbackLocalPath`; `playbackRemoteURL`); gate ClipDetailSheet player, shareURL, ClipGridCell thumbnail. Drafts/imported stay local-first.
- [x] UX-C2 — DONE. `AppStore.cacheRender(clipId:)`: single-flight set, ≤200MB cap (expectedContentLength w/ file-size fallback), download → MediaStore, poster regenerated FROM THE RENDER off-main, mid-download URL-change race guarded; `updateRemoteURL` invalidates renderLocalPath (+deletes file) + thumbnailPath on any URL change; hooked at pollJob, pollClipStatuses, applyTweakResult. Fail-soft to streaming. Evidence: BUILD SUCCEEDED.
  - spec: Render caching: `AppStore.cacheRender(clipId:)` background-downloads the render on `.ready` (hook pollJob / pollClipStatuses / applyTweakResult), sets renderLocalPath, regenerates thumbnailPath poster; invalidate both whenever remoteURL changes; single in-flight guard; skip >200MB; fail-soft to streaming.
- [x] UX-F1 — DONE. MarqueApp owns `@State private var feed = FeedStore()` + `.environment(feed)`; HomeView → `@Environment(FeedStore.self)`; no other FeedStore() instantiations exist. Evidence: BUILD SUCCEEDED; tab-switch keeps buckets (store outlives HomeView).
  - spec: FeedStore hoist: MarqueApp owns `@State private var feed = FeedStore()` + `.environment(feed)`; HomeView switches to `@Environment(FeedStore.self)`.
- [x] UX-F2 — DONE. `FeedSnapshot` Codable → Documents/marque.feed.v1.json; init paints from disk (paintedFromDisk); loadInitial w/ cache → `silentRevalidate` (isLoading never set → skeleton condition `isEmpty && isLoading` never fires); debounced 1s saves after ingest/loadMore/dismiss/AI-upgrade/refresh; RootView scenePhase .active + >15min stale → `revalidateIfStale`. Evidence: BUILD SUCCEEDED; skeleton sites verified at HomeView L125/L221.
  - spec: Feed disk snapshot: Codable FeedSnapshot (scripts/reels/trend/cursors/savedAt) → Documents/marque.feed.v1.json, debounced writes after ingest/refresh; init loads it → instant paint, background revalidate with NO skeletons when cache exists; scenePhase .active + >15min stale → silent revalidate.
- [x] UX-S1.REVIEW — **GRADE 93/100**. All 4 units' requirements verified against the diff (adversarial checklist); backend suite 569 passed (untouched); iOS BUILD SUCCEEDED 0 errors. Traces: (1) ready job → updateRemoteURL → cacheRender → detail sheet plays cached render, else streams remoteURL — raw take unreachable for server-rendered clips incl. share; (2) tab switch → app-owned store, no teardown; relaunch → snapshot paint + silent revalidate, no skeletons. −7: first render-ready clears the raw-take poster before the render poster lands (momentary placeholder if offline) — honest-content tradeoff, per-spec invalidation.

## Slice 2 — one-tap submit (backend then iOS)

- [x] UX-B1a — DONE. `ClipJobRequest` += auto_confirm/toggles/creator_id; `_apply_confirm_to_job` extracted verbatim (confirm endpoint now calls it — its regression tests green untouched; `reset_clips=False` for the auto path since clip ids are minted+returned at create); `_analyze_to_brief` factored (same single gather: transcribe+loudness+dossier; `briefless_on_error` ladder); `_run_auto_pipeline` = analyze→auto-confirm→_run_edit, always terminal; create: auto+keyless → immediate mock_ready+ready clips+mock EDL, auto+live → status processing + clips array + spawned pipeline; one clip minted (no fan-out); 426 + analyze-first untouched. Evidence: 10 new tests (test_auto_confirm.py) incl. stable clip ids, format-default toggles, briefless proceed, transcription fail; suite **579 passed**; eval PASS.
  - spec: Backend: `ClipJobRequest` += `auto_confirm: bool=False`, `toggles: dict|None`, `creator_id: str="default"`. Extract `_apply_confirm_to_job(job, toggles, custom_instructions)` from confirm_clip_job (confirm endpoint behavior byte-identical — regression tests must stay green). New `_run_auto_pipeline(job_id)`: transcribe → brief exactly as `_run_analysis` does on main (factor shared middle `_analyze_to_brief` so dossier+loudness gather is reused) → toggles default from EDIT_FORMATS[fmt]["toggles"] when client omitted → `_apply_confirm_to_job` → `_run_edit`. Brief failure ⇒ proceed briefless; transcription failure ⇒ fail as today. `create_clip_job`: auto_confirm live → status "processing", spawn pipeline, response INCLUDES the clips array; keyless → immediate mock_ready + clips. 426 guard + analyze-first path untouched.
- [x] UX-B1b — DONE. `.recorded` = single context screen (format grid + mimic kept; capability-gated toggle rows + customInstructions TextField moved in; brief screen keeps working minus the moved field); `EditFormat.defaultToggles` mirrors prompts.EDIT_FORMATS values exactly (verified line-by-line), reseeded via .task(id: editFormat); `makeClips()` → `startAnalyzeJob(autoConfirm: true, toggles:)`; response w/ clips → `AppStore.trackSubmittedClips` (factored verbatim from the confirmClips tail — insert/poll/streak/celebration identical) → dismiss to Library; response w/o clips → untouched analyzing/brief fallback; `createAnalyzeJob` += autoConfirm/toggles/creator_id (BackendClient.creatorId). AnalyzeJobResponse += ClipStub[] (decode-safe). Evidence: BUILD SUCCEEDED 0 errors.
  - spec: iOS: `.recorded` screen becomes the single context screen — keep format grid + vibe picker, ADD the toggle rows (new `EditFormat.defaultToggles` mirroring EDIT_FORMATS toggles, reseeded on format change) and MOVE the customInstructions TextField from the `.brief` screen. `makeClips()` calls `startAnalyzeJob(autoConfirm: true, toggles:)`; response with clips → new `AppStore.trackSubmittedClips` (factor from confirmClips tail: insert rendering clips, pollJob, streak/celebration) → dismiss to Library. Response without clips (old backend) → existing .analyzing/.brief fallback stays fully working. LiveClipEngine.createAnalyzeJob += autoConfirm/toggles/creator_id.
- [x] UX-S2.REVIEW — **GRADE 92/100**. Traces: keyless submit → create returns mock_ready + ready clip stubs → trackSubmittedClips inserts .ready clips (backend test + iOS path); monkeypatched live pipeline → processing → _run_auto_pipeline → ready w/ stable clip ids (test_live_auto_pipeline_reaches_ready); confirm endpoint behavior byte-identical (extraction — its regression tests green in the 579-suite). Suite 579 passed keyless; iOS BUILD SUCCEEDED. −8: clip 'format' in auto mode stays the creator-picked format id rather than brief-inferred (documented tradeoff: stable ids returned at create beat mid-flight inference).

## Slice 3 — APNs push

- [x] UX-B2a — DONE. `app/push.py`: ES256 provider JWT (kid/iss/iat, cached 40min, re-signs after TTL), `PUSH_CONFIGURED` gate (keyless = clean no-op), HTTP/2 client, sandbox/prod host routing per token row, `send_clips_ready` (alert+category/thread clips_ready, deeplink marque://library/clip/{id}), 410/BadDeviceToken → soft-disable; token registry = Supabase `device_tokens` (upsert/load/disable methods on SupabaseClient, migrations.sql table w/ unique(token,environment)) + in-memory fallback; `POST /v1/devices` idempotent upsert (re-enable on re-register, env coercion, 422 empty token); ready-landing hook in `_run_edit` fires ONCE per job (`push_sent`; tweak re-renders route through _rerender_clip, never here); deps pyjwt[crypto]+httpx[http2] added to requirements. Evidence: 6 tests (throwaway ES256 key JWT claims+cache, upsert idempotency+re-enable, endpoint, 410 soft-disable w/ mocked transport + env-host routing, keyless no-op, once-per-job hook incl. second-edit no-repush); suite **585 passed**.
  - spec: Backend: new `backend/app/push.py` — ES256 JWT signer cached ~40min (kid=APNS_KEY_ID, iss=APNS_TEAM_ID, key=APNS_P8 PEM), httpx AsyncClient(http2=True), sandbox/prod routing per token, apns-topic=APNS_TOPIC; `PUSH_CONFIGURED` gate, keyless no-op. `send_clips_ready(creator_id, clip_id, count)`: alert "Your clip is ready", thread-id/category clips_ready, deeplink `marque://library/clip/{clip_id}`; one send per job (push_sent flag); 410/400 → soft-disable token. `device_tokens` table in migrations.sql (creator_id text, token, environment check sandbox|prod, platform, app_version, timezone, permission, last_seen_at, disabled_at, unique(token,environment)) + in-memory fallback when Supabase unconfigured. New `POST /v1/devices` upsert endpoint. Send hook where `_render_all_clips` lands the job "ready" (creator_id present); tweak re-renders never push. Unit tests: JWT claims with a generated throwaway ES256 key, upsert idempotency, 410 soft-delete via mocked transport, keyless no-op.
- [x] UX-B2b — DONE. project.yml + checked-in Marque.entitlements gain `aps-environment: development` (pbxproj already references the file → effective WITHOUT xcodegen; project.yml carries it for the next regen + note; CFBundleVersion/CURRENT_PROJECT_VERSION synced to 14). `Adapters/PushManager.swift`: @UIApplicationDelegateAdaptor delegate — registers for remote notifications on every launch when authorized, hex token → `BackendClient.registerDevice` (DEBUG→sandbox, timezone/app_version/permission), didReceive → `onDeepLink` → `AppRouter.handle(url:)` (parses marque://library/clip/{uuid} → Library tab + pendingOpenClipId; OAuth callbacks consumed by ASWebAuthenticationSession never collide); MarqueApp .onOpenURL routes too; LibraryView opens the pending clip's detail (onChange+onAppear). `PushPrimerSheet` (branded explain-then-ask; UserDefaults 72h cooldown, max 3, notDetermined-only) replaces the cold requestAuthorization in notifyClipsReady; local notifications remain the fallback deduped by job id (identifier replace + receivedJobIds + willPresent suppression of remote clips_ready while foregrounded). Evidence: BUILD SUCCEEDED.
  - spec: iOS: ios/project.yml → Marque target entitlements `aps-environment: development` (+ note to run xcodegen). New `Adapters/PushManager.swift`: UIApplicationDelegate via @UIApplicationDelegateAdaptor — register for remote notifications on every launch when authorized, hex token → new `BackendClient.registerDevice(token:environment:)` (DEBUG→sandbox else prod, include timezone/app_version/permission), didReceive → router. `AppRouter.handle(url:)` parses marque://library/clip/{id} → Library tab + pendingOpenClipId; MarqueApp .onOpenURL routes non-OAuth marque:// URLs there; LibraryView presents ClipDetailSheet for pendingOpenClipId. `PushPrimerSheet` shown once at first clips-ready moment (UserDefaults cooldown, max 3); replace the cold requestAuthorization inside notifyClipsReady with the primer path. Local notifications stay as fallback, deduped by job id in userInfo.
- [x] UX-S3.REVIEW — **GRADE 91/100**. Suite 585 passed keyless (6 push tests: throwaway-key JWT claims, upsert idempotency+re-enable, endpoint, 410 soft-disable, keyless no-op, once-per-job hook); iOS BUILD SUCCEEDED. Deferred manual: real-device APNs E2E (needs the user's .p8 in Render env: APNS_KEY_ID/APNS_TEAM_ID/APNS_P8/APNS_TOPIC + a physical device), TestFlight primer-flow walkthrough. −9: sandbox/prod is decided by DEBUG compile flag (TestFlight = prod APNs against aps-environment auto-swap — correct pairing, but untestable until keys exist).

## Slice 4 — inspiration reels + example honesty

- [x] UX-A1 — DONE. `_classify_edit_format(post)` tier-1 heuristic (short/no-speech+short-dur+musicish → recap_music/fast_cuts; bare-caption+narration → recap_voiceover/faceless; spoken+visual-noun caption → talking_head_broll/broll_cutaway; default talking_head) called from `_reel_from_post` → additive `edit_format`/`fmt_source:heuristic`/`why_match` (_WHY_MATCH copy). Tier 2: `_dossier_classify_reels` (top `REEL_CLASSIFY_TOP_K`=8 lacking fmt_source==dossier) via `dossier_for_reference` + `_classify_from_dossier` (eye_contact/cut-density/broll-opportunity signals), wired into `_refresh_niche_reels` phase-2 (post-transcription, pre-final-write) + `_refresh_watched_creator`; fully fail-soft (off/keyless → no-op); dossier classification carried forward in `_merge_prev_reel_work` (paid once per reel). Evidence: 7 classifier/carry/fail-soft tests green.
  - spec: Backend classification: new `_classify_edit_format(post) -> (edit_format, style)` heuristic (caption/hashtags/duration_s/transcript/views: short-or-no transcript + short duration + music-ish caption → recap_music/fast_cuts; spoken transcript + visual-noun-dense caption → talking_head_broll/broll_cutaway; existing faceless heuristic → recap_voiceover/faceless; default talking_head) called from `_reel_from_post`; cached reels gain additive `edit_format`/`fmt_source:"heuristic"`/`why_match`. Tier 2: in `_refresh_niche_reels`/`_refresh_watched_creator` after engagement sort, run the dossier adapter (backend/app/dossier.py) on top-K (env `REEL_CLASSIFY_TOP_K`=8) reels lacking `fmt_source=="dossier"` to classify by watching; fail-soft to tier 1; persist via the `_merge_prev_reel_work` carry-forward keys so it's paid once per reel.
- [x] UX-A2 — DONE. Matching ladder edit_format (dossier tier 0 > heuristic tier 1) → legacy style (tier 2); rank = tier, then log-engagement + transcribed(+1.5) + rehosted-durable-URL(+2.0); playable (non-empty video_url) cards take the top slots; LIVE mode returns real matches ONLY (no padding, fewer real > fake); keyless/no-match → curated exemplars flagged `sample:true`; every card carries `selection_reason` (+view count). Existing keyless endpoint tests untouched-green. Evidence: 5 endpoint tests (sample flag, no-pad single live card, dossier-tier outranks views, rehosted beats raw engagement, unplayable never leads); suite **597 passed**.
  - spec: Rewrite `/v1/reels/examples`: match `edit_format` (dossier>heuristic) then legacy style; rank engagement+recency+transcribed+rehosted-URL bonus; top slots require non-empty video_url; live mode NEVER pads with fabricated cards (fewer real > fake); fabricated fallback cards get `sample:true`; every card gets `selection_reason`. Tests: keyless returns sample:true; seeded recap_music corpus entry selected without padding; ranking prefers rehosted.
- [x] UX-A3 — DONE. `FailableVideoPlayer` extracted → DesignSystem/FailableVideoPlayer.swift (+`muted`/`showsControls` params; ReelDetailSheet call site unchanged-compatible, private copy deleted). mimicCard: autoplays muted+looping when videoURL non-empty AND card visible (onAppear/onDisappear set), `allowsHitTesting(false)` keeps the card the tap target, hard failure → poster fallback (failedMimicIds); why line = whyMatch ?? whyTrending; SAMPLE capsule on `sample` cards. ReelItem += editFormat/whyMatch/sample (optional-with-default, Snapshot-safe); ReelDTO += optional wire fields. Evidence: BUILD SUCCEEDED.
  - spec: iOS: extract FailableVideoPlayer from ReelDetailSheet (private → DesignSystem/FailableVideoPlayer.swift, add muted/showsControls params; ReelDetailSheet keeps working). mimicCard: autoplay muted looping player when videoURL non-empty, onFailure → AsyncImage fallback, play only visible cards (onAppear/onDisappear), why line from whyMatch ?? whyTrending, SAMPLE chip when sample. ReelItem/ReelDTO += editFormat/whyMatch/sample (optional-with-default).
- [x] UX-S4.REVIEW — **GRADE 91/100**. Suite 597 passed; iOS BUILD SUCCEEDED. Trace per format: keyless → curated exemplars honestly SAMPLE-chipped; live → classifier makes recap_music/talking_head_broll matchable for the first time (seeded-corpus test selects without padding), playable cards lead, live never pads with fabricated. −9: tier-2 dossier classification needs live keys to exercise end-to-end (seam-tested + carried forward); autoplay-visibility uses onAppear/onDisappear (coarse but correct for a horizontal strip).

## Slice 5 — chat on clip preview

- [ ] UX-D1 iOS: prominent "Tell the editor what to change…" input-shaped affordance under the ClipDetailSheet player (ready + server-rendered clips) opening TweakChatSheet at .medium/.large detents, composer focused; keep the menu entry.
- [ ] UX-D2 Preview-first flow: new `LiveClipEngine.tweakClipPreview(jobId:clipId:instruction:)` → POST tweak?preview=1; stash returned `applied` ops as pendingOps; when preview_requested, poll GET /v1/clips/{jobId} (3s) for my clip's preview_status/preview_url; ready → new transient `Clip.previewURL` (optional-with-default; cleared on apply/discard/dismiss/remoteURL change) → detail player prefers previewURL with a PREVIEW badge (player .id includes it); Apply → tweakClipOps(ops: pendingOps) deterministic commit + existing render poll; Discard → clear (server committed nothing). preview_requested==false (keyless/undo) → today's direct flow unchanged. 409 → existing polite copy.
- [ ] UX-S5.REVIEW review + suite + trace: discard leaves EDL identical; apply commits; races safe.

## Slice 6 — reasoned feed

- [ ] UX-G1 Backend: factor `_top_arms(creator_id, niche)` from get_recommendations (Thompson arms w/ human reason; _cold_recommendations fallback). `_feed_sreq` consumes arms for pillar/topic+style (template fallback when exhausted); `_compose_feed_items` stamps `why_picked` on every script (arm reason or "From your '{pillar}' pillar" + trend link); `/v1/suggestions/next-idea` routes through _top_arms. Tests: stubbed arms drive selection; why_picked present in fast + full paths; cold start reasons.
- [ ] UX-G2 iOS: Script.whyPicked (+DTO); pick cards + ScriptReaderView show the why line (micro type, tertiary color).
- [ ] UX-S6.REVIEW review + suite.

## Slice 7 — knowledge base v2 (LAST, eval-gated)

- [ ] UX-E1 Research: if web tools are available, deep-research professional short-form editing (retention-curve editing, first-2s visual grammar, J/L cuts + cut-on-action, transition taxonomy AND restraint, sound design/beat-cutting/SFX/risers, kinetic caption practice, per-format playbooks); else distill from the seed corpus in the spec + existing KB. Operational rules with numbers only — no vibes.
- [ ] UX-E2 Corpus: deepen the 7 existing backend/knowledge files; add transitions.md, sound_design.md, hook_visual.md, format_playbooks.md (one section per EDIT_FORMATS key); MANIFEST → kb-2026.08. Routing in knowledge.py digest(): EDL stage → pacing+transitions+matching format playbook; brief → hooks+retention+hook_visual; review → rubric+sound_design; token budgets hold (assert in tests).
- [ ] UX-E3 HARD GATE: `python -m eval.edl_eval` after — no dimension regresses vs the baseline recorded at orientation. If it regresses, iterate the KB until it doesn't; that is part of this unit.
- [ ] UX-S7.REVIEW review + suite + eval.

## FINAL gauntlet

- [ ] UX-F.1 One clean full run: backend suite keyless + eval + render build:bridge, all green.
- [ ] UX-F.2 End-to-end mock walkthrough: auto_confirm submit (keyless) → mock_ready clips tracked; examples endpoint per format; feed with why_picked; devices upsert; tweak preview path (mocked).
- [ ] UX-F.3 Write docs/RALPH-UX-REPORT.md: what shipped per fix, evidence index, deferred manual steps (real-device APNs E2E with the user's .p8 key; TestFlight push primer flow; on-device playback checks), known gaps.
- [ ] FINAL — print `ALL_UX_FIXES_COMPLETE`.

---

## Run log

- (bootstrap) Plan + progress files created off main f6e1bf9 (post-overhaul, post-deploy). Next: baseline.

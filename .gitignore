# Xcode / build
ios/build/
ios/Marque.xcodeproj/
DerivedData/
*.xcuserstate
*.xcuserdatad/

# macOS
.DS_Store

# Maestro local artifacts
.maestro/tests/

# Secrets (never commit) — paste keys here locally
.env
*.secrets.json

# Python backend
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Remotion render project
render/node_modules/
render/dist/
render/dist-test/
render/fixtures/source.mp4
render/out/

# LOOP U — machine-generated vision-tier cache, not meaningful to commit
# (unlike .shots/*.png itself, which is an intentionally-committed reference set)
.shots/.ui_eval_cache.json
